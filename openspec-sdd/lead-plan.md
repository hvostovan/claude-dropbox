# План Team Leader — техническая подготовка и ведение обучения

> Отдельно от `team-training-plan.md` (тот — про роли и общий процесс). Это — конкретно твой
> операционный план: что настраиваешь, что запускаешь, какими командами. Содержимое
> контекст-файлов ниже — реальные шаблоны из материала Сбербанка, подрезанные под наш
> минимальный набор. Раз используем только `start-here` + `architecture` +
> `engineering-standards` (без `product-context/domain-glossary/security-and-compliance/
> testing-and-quality/anti-patterns` — решили их не создавать, роли сформируют свою часть в
> ходе упражнения), пронумеровали подряд без дырок: `00-start-here.md`, `01-architecture.md`,
> `02-engineering-standards.md`.
>
> Задача зафиксирована — см. `task.md` (планировщик спринтовых задач, 3 команды/3 стека).
> `01-architecture.md` ниже заполнен целиком — инварианты общие для всех веток, они про домен,
> не про стек. `02-engineering-standards.md` — три готовых варианта (Python/JS/Java), в каждую
> ветку кладёшь свой. Из плейсхолдеров остались только даты/[название файла проекта] — по
> вкусу.

## Роль в двух словах

Готовишь инфраструктуру и рамку (архитектурные ограничения), ведёшь процесс, следишь, что
каждый передаёт следующей роли письменный артефакт, а не пересказ на словах. Задачи за роли не
решаешь.

## До Дня 1: пошагово

**1. Создать репозиторий** для командного упражнения (отдельный от `claude-dropbox` — это
боевой репо команды на время обучения).

**2. Установка проекта** (пример для Node.js — под свой стек в другой ветке команда адаптирует
инициализацию под себя, например `python -m venv .venv` вместо `npm init`):
```
mkdir <project> && cd <project>
npm init -y
openspec init
mv .qwen .gigacode   # в меню выбрать Qwen (пробел), затем Enter
```
Зависимости (`npm install ...` / `pip install ...` и т.п.) ставим не сейчас вслепую, а по ходу
разработки — когда станет понятно, что именно нужно под задачу.

**3. Создать папку контекста и три файла вручную** (без скачивания архива):
```
mkdir -p openspec/context
```
Дальше создать три файла — содержимое см. ниже.

**4. Заменить `openspec/config.yaml`** на версию ниже — там список файлов подрезан под наш набор.

**5. Заполнить плейсхолдеры** `[в квадратных скобках]` реальным содержанием — как только пришлёшь
задачу, впишу конкретику вместо плейсхолдеров сам.

**6. Проверить, что контекст читается:** прогнать `gigacode` один раз, спросить агента что-то
вроде "что ты знаешь про архитектуру проекта" — должен процитировать `01-architecture.md`.

**7. Проверить окружение** (Node.js, npm, GigaCode CLI, доступ к репозиторию).

## Содержимое файлов

### `openspec/context/00-start-here.md`
```markdown
---
owner: Тимлид (context owner)
updated: [ГГГГ-ММ-ДД]
---
# Start Here
> Только навигация и главные запреты, не само знание. Держать на один экран.

Работаешь с продуктом: планировщик спринтовых задач (эпики → задачи → исполнители).

## Перед началом любой задачи
1. Прочитай 01-architecture.md.
2. Прочитай 02-engineering-standards.md.
3. Не делай изменений вне scope задачи.
4. Не хватает контекста — сформулируй, какого, и не угадывай.

## Главные запреты
- Не подключать БД — хранение только в памяти процесса.
- Не подключать отдельный JS UI-фреймворк — рендеринг только на сервере.
```

### `openspec/context/01-architecture.md`
```markdown
---
owner: Архитектор / Тимлид
updated: [ГГГГ-ММ-ДД]
---
# Architecture
> Общие инварианты для всех трёх команд (Python/JS/Java) — про домен, не про стек.

## Инварианты
1. Данные (эпики, задачи) хранятся только в памяти процесса — без БД. Сброс при перезапуске
   для MVP осознанно допустим.
2. Задача с несколькими исполнителями размножается: отдельная запись на каждого исполнителя,
   а не одна задача "на двоих".
3. Задача не обязана быть привязана к эпику — допустима задача без привязки ("общая").
4. Рендеринг — только на сервере (server-side), полными HTML-страницами. Без отдельного
   JS-фреймворка на фронте и без AJAX/fetch — формы отправляются обычным POST с перезагрузкой
   страницы.

## Обязательные / запрещённые паттерны
- Обязательно: server-side rendering (Jinja2 / EJS-Pug / Thymeleaf — своё под ветку).
- Запрещено: подключать React/Vue/аналоги или БД — усложняет сравнение SDD-процесса между
  командами и не нужно для MVP.

## Схема
Нет отдельной схемы — архитектура одна и та же во всех трёх ветках (backend + server-side
шаблонизатор + in-memory хранилище), различается только языком/фреймворком. Детали продукта и
модель данных — см. `task.md` в `claude-dropbox`.

## Ключевые интеграции и зависимости
Нет — MVP без внешних систем (ни БД, ни трекера). Интеграция с API корпоративного трекера для
автозаведения задач — следующий этап, вне рамок текущего MVP.
```

### `openspec/context/02-engineering-standards.md` — три варианта, по одному на ветку

**Python-ветка:**
```markdown
---
owner: Тимлид
updated: [ГГГГ-ММ-ДД]
---
# Engineering Standards
Стек: Python 3.12, FastAPI + Jinja2Templates, Uvicorn.
Стайл-гайд: PEP8 + ruff.

Запуск проверок:
- Тесты: pytest
- Линт: ruff check .
- Сборка: не требуется, запуск напрямую через uvicorn main:app

Код готов, когда: pytest зелёный, ruff check без ошибок, страницы рендерятся и формы
отправляются без JS.
```

**JavaScript-ветка:**
```markdown
---
owner: Тимлид
updated: [ГГГГ-ММ-ДД]
---
# Engineering Standards
Стек: Node.js 20+, Express + EJS (server-side шаблоны).
Стайл-гайд: eslint + prettier.

Запуск проверок:
- Тесты: npm test
- Линт: npx eslint .
- Сборка: не требуется, запуск напрямую через node server.js

Код готов, когда: npm test зелёный, eslint без ошибок, страницы рендерятся и формы
отправляются без клиентского JS.
```

**Java-ветка:**
```markdown
---
owner: Тимлид
updated: [ГГГГ-ММ-ДД]
---
# Engineering Standards
Стек: Java 21, Spring Boot (Spring Web + Thymeleaf), Maven.
Стайл-гайд: Google Java Style.

Запуск проверок:
- Тесты: mvn test
- Сборка/запуск: mvn spring-boot:run

Код готов, когда: mvn test зелёный, приложение поднимается через spring-boot:run, формы
отправляются без JS.
```

### `openspec/config.yaml`
```yaml
# openspec/config.yaml
# Связка контекста с OpenSpec: context инжектится во ВСЕ артефакты,
# rules — только в свой артефакт.

schema: spec-driven

context:
  Продукт: Планировщик спринтовых задач. Веб-UI для PO — эпики, задачи, исполнители,
    результат MVP — провалидированный список задач (без интеграции с трекером).
  Стек: [заполнить под ветку — Python/FastAPI, JS/Express или Java/Spring Boot, см. task.md].

Если описание в _raw расходится с актуальным кодом — верь коду, спорное выноси человеку.

Перед созданием ЛЮБОГО артефакта обязательно прочитай файлы из openspec/context/:
- 00-start-here.md, 01-architecture.md, 02-engineering-standards.md
Не хватает контекста — НЕ угадывай, сформулируй, какого, и не молчи.

rules:
  proposal:
    - Всегда заполняй Scope и Non-goals.
    - Противоречие требований — подними вопрос человеку.
  specs:
    - Если описание в _raw расходится с кодом — верь коду.
  design:
    - Опирайся на инварианты из 01-architecture.md.
  tasks:
    - Не делай refactor вне задачи.
```

## Шпаргалка команд

| Шаг | Команда |
|---|---|
| Создать проект | `mkdir <project> && cd <project>` |
| Инициализировать (пример Node) | `npm init -y` (для Python — `python -m venv .venv`, для Java — Spring Initializr) |
| Инициализировать OpenSpec | `openspec init` |
| Переключить на GigaCode | `mv .qwen .gigacode` (в меню выбрать Qwen) |
| Запустить GigaCode | `gigacode` |
| Обсудить требования | `/opsx-explore "..."` |
| Создать спецификацию | `/opsx-propose "..."` |
| Сгенерить/изменить код по спеке | `/opsx-apply` |
| Закрыть задачу | `/opsx-archive` |

## В процессе (День 1 / День 2) — что делаешь руками

- Держишь шпаргалку под рукой, чтобы подсказывать команды, если кто-то завис.
- На стыках ролей проверяешь, что артефакт реально записан в файл (не осталась в устной
  форме) — см. чек-поинты в `team-training-plan.md`.
- Если участник не может продвинуться — не решаешь за него, а задаёшь наводящий вопрос по
  SDD-цепочке ("а что из этого попало в спеку?").

## Осталось по каждой ветке (когда заведёшь branches в своём репозитории)

- [ ] Скопировать `01-architecture.md` как есть — общий для всех трёх
- [ ] Положить в `02-engineering-standards.md` вариант под стек этой ветки (Python/JS/Java —
      см. выше)
- [ ] В `config.yaml` заполнить `Стек:` под конкретную ветку
- [ ] Уточнение, нужны ли ещё материалы Сбербанка в процесс — по желанию, не блокирует старт
